name: My First WorkFlow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: welcome message
          run: echo "My first github action job"

        - name: List the file
          run: ls

        - name: Read the file
          run: cat README.md